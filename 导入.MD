# 四部计划导入规则

## 导入范围

- 来源文件：`E:\KaiNice\工作\凯南\02_PMC\主计划表\追踪表\四部计划.xls`
- 来源工作表：`8.5`
- 目标：凯南计划中心的 2026 年 8 月月度计划，以及由该批数据生成的滚动主计划。
- 本次全量替换前，先备份计划相关表。
- 导入前清空全部滚动主计划/月度计划数据，包括订单、计划月份、月度明细、外协明细和工序进度；用户、角色、供应商、字典、工序定义、销售订单和成品入库等基础资料不受影响。
- 清空与写入在同一数据库事务中完成；数据库级错误会回滚到导入前状态。
- 任何单行或单字段异常均记录后跳过对应行或字段，不中断其余数据导入；数据库级错误会回滚整个事务。

## 基础字段对应

| 原表字段 | 月度计划字段 | 转换规则 |
| --- | --- | --- |
| 订单号 | 订单号 | 去除首尾空格；缺失时跳过整行 |
| 下单日期 | 下单日期 | 转为 `YYYY-MM-DD` |
| 订单交期 | 异常后二次交期 | 转为 `YYYY-MM-DD` |
| 新旧款 | 新旧款 | 原值严格等于“新”时写入“新”，其他值均写入“旧” |
| 品号 | 品号 | 为空时不导入；与订单号共同判定重复行 |
| 订单号 + 品号 | 关联信息 | 直接连接为字符串，不允许手工编辑 |
| 品名 | 品名 | 正常导入 |
| 生产数量 | 订单需求数量 | 按数字导入，允许小数 |
| 系统入库数据 | 历史入库数据 | 按数字导入，允许小数 |
| 生产报表数据 | 当天入库数 | 按数字导入，允许小数 |
| 对应计划页数 | 对应计划页数 | 按数字导入 |
| 订单异常信息 | 订单异常信息 | 正常导入 |
| 装柜 | 装柜日期 | 转为 `YYYY-MM-DD` |
| 验货 | 验货 | 正常导入 |
| 验货数量 | 验货数量 | 按数字导入 |
| 备注 | 备注 | 正常导入 |
| 订单周数 | 订单周数 | 按数字导入 |
| 单价 | 单价 | 按数字导入，允许小数 |
| 客户 | 客户 | 正常导入 |
| 月 | 年月 | 来源值不直接使用，统一写入 `2026-08` |
| — | 所属事业部 | 统一写入“事业四部” |

## 外协字段转换

- 原表“外协方式”为空：计划“制作方式”写入“自制”。
- 原表“外协方式”非空：计划“制作方式”写入“外协”。
- 原表“外协方式”为“毛坯”：计划“外协方式”写入“毛坯”。
- 原表“外协方式”为“外协”或“完成”：计划“外协方式”写入“成品”。
- 原表“外协方式”为“申请外协”：计划“外协方式”留空。
- 其他未定义的外协方式：制作方式仍写入“外协”，外协方式留空并记录异常。
- 原表“外协供应商”包含“未外发”或“未下外协”时，计划外协供应商留空；其他值正常导入。
- 原表“生产单位”不导入。

## 工序完成时间转换

原表以下九列逐一对应月度计划同名工序：图纸&BOM、前道配件、机加、焊接/点焊、研磨、毛坯、烤漆/电镀、后道包材&配件、组装&包装。

- 值为 `OK`：
  - 图纸&BOM、前道配件、机加、焊接/点焊、研磨、后道包材&配件的“状态/数量”写入 `Y`。
  - 毛坯、烤漆/电镀、组装&包装的“数量”写入该行订单需求数量，状态由系统按数量和交期规则自动计算。
- 值为有效日期：写入对应工序“交期”。
- 值既不是 `OK` 也不是有效日期：原值写入对应工序“异常”。
- 值为空：对应工序不写入进度记录。

## 不直接导入的字段

- 简图：原表使用 `DISPIMG`/嵌入对象，未按文件附件导入。
- 订单欠数、订单入库金额、订单欠数金额：由系统根据数量和单价实时计算。
- 生产单位：按要求忽略。
- 原表“8/7最新更新”“进度备注”“最晚出货时间节点”“8/6下午4点出货不足明细”“最终出货信息备注”等月度计划无对应字段的尾部列：不导入。

## 异常和重复处理

- 品号为空：按规则跳过，不记为导入异常。
- 订单号为空但品号存在：跳过整行并记录异常。
- 相同“订单号 + 品号”在来源表重复：保留首次出现的行，跳过后续重复行并记录来源行号。
- 日期或数字无法识别：只跳过该字段，保留其他可用字段并记录异常。
- 同一订单的订单级字段出现多个值：订单主记录采用首次出现的非空值；月度计划明细保留各自行值，并记录警告。
