# 主计划系统测试报告 v2

## 1. 最终结论

原报告的 4 个 FAIL 已全部修复、迁移、部署并核验，总体结论为 **PASS**。当前运行环境可直接使用表格选择与批量修改、稳定审计身份、事务型同步 outbox、租户内跨表引用，以及按权限控制的主计划 Excel 导入导出。

## 2. 原 FAIL 闭环

| 原问题 | 修复结果 | 验证 |
| --- | --- | --- |
| 写入与后续同步非原子 | 业务写入和 `mps_reconciliation_outbox` 同事务提交；提交后消费失败可重试 | 真实任务进入 outbox 并处理为 `SUCCESS`，尝试次数 1 |
| 跨表引用不完整 | 月计划引用订单分配复合业务键；出货计划保存租户内稳定月计划 UUID | 两个数据库外键存在；3 条匹配出货记录完成回填 |
| 审计身份不稳定 | 18 张业务表 `updated_by` 迁移为 `users.id` UUID；创建固定禁用系统主体 | MPS 范围 19/19 个 `updated_by` 列均为 UUID（含 outbox） |
| 主计划缺少导入导出 | 新增按资源导出、模板、预览、确认接口及前端入口 | 路由注册、权限拒绝、加密文件提示、导出字段测试通过 |

## 3. 数据与安全验证

- Migration：`MasterPlanIntegrityAndOutbox1722920048000`、`MasterPlanShippingMonthlyReference1722920049000` 已先在恢复副本验证，再应用于当前数据库。
- RLS：`mps_reconciliation_outbox` 已启用。
- 导入：只更新带“记录ID + 版本”的已有行；预览与确认均校验租户、用户、资源、操作权限、字段权限、数据范围和乐观版本；确认整批事务提交并记录逐行与批次审计。
- 加密检测：解析工作簿前调用统一检测，准确返回“该文件被加密,请解密后再导入.”。
- 导出：复用页面搜索、筛选、排序和数据范围，按字段可见权限导出全部匹配页。
- 历史异常：6 条缺月计划的浏览器测试记录被原样保留并登记于 `outputs/主计划系统历史异常清单_20260911.md`。

## 4. 自动化与构建

| 验证项 | 结果 |
| --- | --- |
| API Jest | 35 套、158 项通过 |
| 主计划 API 定向测试 | 3 套、14 项通过 |
| Web Vitest | 15 个文件、59 项通过 |
| API lint / typecheck / build | 通过 |
| Web lint / typecheck / build | 通过；仅保留既有 Fast Refresh 与大 chunk 警告 |
| 迁移副本演练 | 两次新增迁移均通过 |

本机 Node 为 v22，而仓库声明 Node >=24；正式镜像使用 Node 24 并构建成功，因此不影响当前部署。

## 5. 部署与备份

- 运行地址：`http://127.0.0.1:15172`。
- 最终健康：API、Web、PostgreSQL 容器均 healthy；`GET /api/v1/health` 返回 `status=ok`。
- 迁移前备份：
  - `data/backups/four_department_tracker_20260911_213829.backup`
  - `data/backups/four_department_tracker_20260911_214547.backup`
  - 同批次 `kdos` 和 `uploads` 备份均已生成并校验 SHA256。
- 最新数据库迁移：`MasterPlanShippingMonthlyReference1722920049000`。

## 6. 仍保留的非阻塞说明

- 6 条历史测试出货计划因刻意使用不存在的品号，不能自动建立月计划引用；已保留、隔离并记录，不影响新数据完整性。
- 未取得真实业务账号明文密码，因此没有在报告中伪造带登录态的人工导入确认；控制器路由、权限拒绝、服务单测、事务逻辑及线上构建均已验证。

