# 待确认业务与后续决策

1. 产前评审交期颜色口径仍采用：完成率 ≥ 100% 绿色、等于当天黄色、早于当天红色；请业务方最终确认。
2. 月初滚动当前复制上一月份正式版（无正式版时使用 Draft）中欠数大于 0 的品号。除“上月未完成”外的未来计划来源需要业务方定义。
3. Keycloak 已有 Provider/Identity 数据边界但未部署；生产切换需要确认域名、Realm、客户端、MFA 和账号迁移方案。
4. 静态图片当前由 LocalObjectStorage/Nginx 提供；如图片需要细粒度私有访问，应在下一阶段采用鉴权下载或签名 URL。
5. 旧 `four_department_tracker` 数据库和 TypeORM 页面仍在迁移兼容期。完成用户验收后，由用户决定退役时间和只读归档方式。
